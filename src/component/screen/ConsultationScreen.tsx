import React from 'react';
import type {PropsWithChildren} from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

function ConsultationScreen():JSX.Element{
    return(
        <ScrollView>
        <Text>HI</Text>
        </ScrollView>
    );
}

export default ConsultationScreen;